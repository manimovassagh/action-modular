name: Main Workflow

on:
  push:
    branches:
      - main

jobs:
  run-main-part:
    uses: ./.github/workflows/job1.yml
    with:
      param1: "Hello from Job 1"

  call-job2:
    uses: ./.github/workflows/job2.yml
    with:
      param2: "Hello from Job 1"
