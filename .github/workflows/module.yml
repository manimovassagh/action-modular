name: Main Workflow

on:
  push:
    branches:
      - main

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - name: Run Job 1
        uses: ./.github/actions/job1
        with:
          param1: "Hello from Job 1"

      - name: Run Job 2
        uses: ./.github/actions/job2
        with:
          param2: "Hello from Job 2"
