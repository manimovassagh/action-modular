name: Main Workflow

on:
  push:
    branches:
      - main

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run Job 1
        uses: ./.github/actions/summer

      - name: Run Job 2
        uses: ./.github/actions/sec
